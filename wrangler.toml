name = "newyorkcomputeclub"
main = "src/worker.js"
compatibility_date = "2026-08-01"

[assets]
directory = "./public"
binding = "ASSETS"

# The email list. Create it once:
#   npx wrangler kv namespace create SIGNUPS
# then paste the id it prints below and uncomment both lines. Until then,
# the site deploys fine and signups answer "list is not wired up yet".
#
# [[kv_namespaces]]
# binding = "SIGNUPS"
# id = "PASTE_KV_NAMESPACE_ID_HERE"
